# Install the PyTorch/PyG native stack with scripts/fix_pyg_env.sh first so the
# extension wheels match your local torch and CUDA build. This file intentionally
# avoids pinning stale torch-scatter / torch-sparse / torch-cluster wheels.

torch_geometric
lightning==2.2.5
pytorch_lightning==2.2.5
hydra-core
python-dotenv
pyrootutils
wandb
rich
hydra-optuna-sweeper
hydra-colorlog
ase
scikit-learn
pandas
rdkit
omegaconf
PyYAML
e3nn
